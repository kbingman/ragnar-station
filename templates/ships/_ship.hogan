{{# starships }}
  <div id="ship-{{ id }}">
    <a href="/ships/{{ id }}" class="r-action" data-edit="{{ id }}">
      {{ name }}
    </a>
    {{ mass }} tonnes {{ configuration }}
    <a class="r-delete r-action" data-delete="{{ id }}">X</a>
  </div>
{{/ starships }}
